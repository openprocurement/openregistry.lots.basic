---
plugins:
  api:
    plugins:
      lots.core:
        plugins:
          lots.basic:

config:
  auth:
    type: file
    src: auth.ini
  database:
    couchdb.db_name: tests
    couchdb.url: http://op:op@localhost:5984/
